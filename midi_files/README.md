# MIDI Files Directory

This directory is designated for storing MIDI files used in the Scaling Law Musical MIDI Data ML Project.

## Purpose

- Store input MIDI files for data processing
- Store generated MIDI files from the models
- Organize MIDI data for training and evaluation

## Usage

Place your MIDI files (`.mid` or `.midi` format) in this directory for processing by the project notebooks.
